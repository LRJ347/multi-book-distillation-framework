---
name: <skill-slug>                # kebab-case,唯一
description: |                    # A2 浓缩版, ≤300 字
  <何时用 + 何时不用 + 关键 trigger 语言信号>
version: 1.0.0
source_books:
  - <书名 1>(第 X 章, L4)
  - <书名 2>(第 Y 章, L3)
semantic_invariants:              # V4 前置,蒸馏前定义
  - 必须保留的条件
  - 必须保留的因果方向
  - 必须保留的适用对象
  - 必须保留的反例
verification_layers_passed:
  - V-REF: ⏳
  - V-REAL: ⏳
  - V-NEG: ⏳
  - V-E2E: ⏳
  - V4: ⏳
related_skills:
  direct: []
  transitive: []
tags: [<领域标签>]
---

# Skill Card(决策入口)

```yaml
trigger:
  positive:
    - 用户说"<具体话>"时
    - 用户问"<具体问题>"时
  negative:
    - 用户说"<诱饵>"时不触发
decision_question: "<用户当前问题是否实际在问本 skill 所解决的问题?>"
input_required:
  - <需要用户提供的输入>
output_type: <输出形态>
confidence:
  high: <什么情况下高置信>
  medium: <什么情况下中置信>
  low: <什么情况下低置信>
```

# <Skill 名称>

## R(原文引用)
> "<原文,≤150 字>"——<书> 第 <X> 章
> "<原文,≤150 字>"——<书> 第 <Y> 章
(每条 ≤150 字,标注章节;英文原书引用原文 + 自己翻译)

## I(方法论骨架)
<用自己的话重写核心骨架,5-15 行。检查: 没读过原书的人能否理解?>

## A1(书中案例)
- 案例 1: <书>: <问题> → <方法> → <结论> → <结果>
- 案例 2: ...

## A2(触发场景)
- 场景 1: 用户说"<具体语言信号>"时
- 场景 2: ...
(每个场景必须有可识别的语言信号,避免宽泛)

## E(执行步骤)
1. 步骤 1(完成标准: <可判断>)
2. 步骤 2(完成标准: <可判断>)
3. 判停点: <若 X 则跳到步骤 5>

自查 checklist:
- [ ] <检查 1>
- [ ] <检查 2>

## B(边界)
- 反场景 1: <何时不要用>
- 反场景 2: <作者警告的失败模式>
- 与相邻 skill 的区分: <易混淆但不同的方法论>

## 适用条件(若存在条件冲突)
- 在 <条件 A> 下: <方案 X>
- 在 <条件 B> 下: <方案 Y>

## 相关 skills
- <skill-A>: 依赖/对比/组合关系
- <skill-B>: ...
